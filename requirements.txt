# Core
fastapi
uvicorn[standard]
python-dotenv
pydantic

# RAG Pipeline
langchain
langchain-community
langchain-text-splitters
langchain-groq
chromadb
# CPU-only PyTorch (Railway runs CPU-only; avoids 3GB+ CUDA wheels)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers

# Data Ingestion (HTML only — Groww pages)
beautifulsoup4
requests

# Utilities
tiktoken

# Testing
pytest
httpx

# Pinned for Python 3.9 Windows compatibility
greenlet==3.0.3
